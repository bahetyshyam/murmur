<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
<dict>
  <!-- Electron/V8 needs JIT + writable-executable memory under hardened runtime. -->
  <key>com.apple.security.cs.allow-jit</key>
  <true/>
  <key>com.apple.security.cs.allow-unsigned-executable-memory</key>
  <true/>
  <!-- Self-signed (no Apple Team ID): the Electron framework + our .node addons
       aren't signed by the same authority Apple expects, so library validation
       must be disabled or the app won't launch. -->
  <key>com.apple.security.cs.disable-library-validation</key>
  <true/>
  <!-- Microphone capture (getUserMedia in the capture host). -->
  <key>com.apple.security.device.audio-input</key>
  <true/>
</dict>
</plist>
