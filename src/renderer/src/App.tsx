export function App(): JSX.Element {
  return (
    <div className="app">
      <h1>Murmur</h1>
      <p className="subtitle">Tap-to-talk dictation — Electron rewrite (Phase A shell).</p>
      <p className="muted">Settings, API key, microphone, hotkey, history, and onboarding land in the phases ahead.</p>
    </div>
  )
}
