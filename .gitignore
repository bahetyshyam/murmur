# macOS
.DS_Store
._*

# Python
.venv/
venv/
__pycache__/
*.py[cod]
*.egg-info/
build/
dist/
.pytest_cache/
.mypy_cache/
.ruff_cache/

# Swift / SPM
.build/
.swiftpm/
# Package.resolved IS committed: this is an app (not a library), and pinning
# the resolved Sparkle version keeps local and CI builds reproducible.
*.xcodeproj/xcuserdata/
*.xcworkspace/xcuserdata/
DerivedData/

# Built app bundle (artifact of scripts/build)
VoiceToText.app/

# Secrets / local config
.env
.env.*
*.local

# Local design references (competitor screenshots, inspiration — not shipped)
design-references/

# Editors
.vscode/
.idea/
*.swp
.secrets/
